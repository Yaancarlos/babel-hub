export interface Period {
    id: string;
    name: string;
    start_date: string;
    end_date: string;
}

export interface CreatePeriod {
    id: string;
}

export interface UpdatePeriod {
    id: string;
}